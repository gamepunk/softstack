---
install:
  homebrew: brew install --cask alacritty
name: Alacritty
tagline: A GPU-accelerated terminal emulator. Fast, minimal, and configured with a text file.
description: |
  Alacritty is a terminal emulator built for speed. It renders with the GPU via OpenGL, which means scrolling through massive logs is butter-smooth. There's no GUI settings panel — configuration is a well-documented YAML file, which appeals to developers who want their terminal config version-controlled.

  It's free, open-source, and cross-platform. The philosophy is "do one thing well": it's a terminal, not an IDE, not a multiplexer — just the fastest terminal you'll ever use.
website: https://github.com/alacritty/alacritty/
category: developer
platforms: [mac, linux]
type: gui
price: open-source
version: "0.17.0"
featured: false
---
