---
name: Everything
tagline: Instant file search for Windows. Finds any file on your drives in milliseconds.
description: |
  Everything by voidtools is the file search tool Windows should have shipped with. It indexes your entire file system and returns results instantly as you type — no waiting, no "searching..." progress bar. Finds files by name across all your drives in milliseconds.

  It's tiny (under 2MB), uses minimal RAM, and can be run as a portable app. Supports regex, boolean operators, and advanced search syntax. The free version covers everything most users need.
website: https://www.voidtools.com
category: utility
platforms: [windows]
type: gui
price: free
featured: false
---
